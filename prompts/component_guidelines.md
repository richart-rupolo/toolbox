Prompt for Cursor:

- Create a new React component in TypeScript based on the description below:
- Follow Clean Code and Code Calisthenics rules:
  - Small and reusable components
  - Well-typed props
  - Pure functions where possible
  - Clear naming and separation of concerns
- Example description: "A button component with loading state and optional icon."
- Provide only the TypeScript code, ready to use.
